cmake_minimum_required(VERSION 3.20)
project(redot_kicker_credential_helper VERSION 1.0.0 LANGUAGES C CXX)

if(WIN32)
  add_executable(redot-kicker-credential-helper windows/main.cpp)
  target_compile_features(redot-kicker-credential-helper PRIVATE cxx_std_17)
  target_compile_definitions(redot-kicker-credential-helper PRIVATE UNICODE _UNICODE WIN32_LEAN_AND_MEAN NOMINMAX)
  target_link_libraries(redot-kicker-credential-helper PRIVATE Advapi32)
elseif(UNIX AND NOT APPLE)
  find_package(PkgConfig REQUIRED)
  pkg_check_modules(LIBSECRET REQUIRED IMPORTED_TARGET libsecret-1)
  add_executable(redot-kicker-credential-helper linux/main.c)
  target_compile_features(redot-kicker-credential-helper PRIVATE c_std_11)
  target_link_libraries(redot-kicker-credential-helper PRIVATE PkgConfig::LIBSECRET)
else()
  message(FATAL_ERROR "Only Windows and Linux x86-64 helpers are in the initial release scope")
endif()

install(TARGETS redot-kicker-credential-helper RUNTIME DESTINATION bin)
